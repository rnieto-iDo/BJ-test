/** Array consts */
export const FIRST_ELEMENT_INDEX = 0;

// REGEXS

export const PHONE_NUMBER_REGEX = /^\+?[0-9]*$/;
export const EMAIL_REGEX = /[a-z0-9]+@[a-z]+\.[a-z]{2,3}/;
export const NUMBER_REGEX = /(^[0-9]+$|^$)/;
export const BOTH = 'Both';
export const PRODUCT_QUOTE = 'Product Quote';
export const MATERIAL_SAMPLE = 'Material Sample';
